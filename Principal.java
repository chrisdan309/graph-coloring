import DataStructure.Graph;
class Principal{
    public static void main(String[] args) {
        int numVertices = 5;
        Graph graph = new Graph(numVertices);
        graph.colorGraph(3);

    }
}

